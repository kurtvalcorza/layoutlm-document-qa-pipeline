# Release status

Current status: **Candidate — tutorial source complete, local pre-flight executed, awaiting supported-runtime execution**. The pipeline package with its adaptation contract (`check_fit`, `evaluate`, `adapt`, `save_artifact`, `from_artifact`), the offline unit suite (including the dataset-contract, role-helper, import-boundary and notebook-parity suites), the model-backed regressions (CPU and CUDA), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial `tutorials/layoutlm_document_qa_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks PAR1–PAR3); one fresh-kernel execution on the workstation is recorded in `docs/release-verification.md`. None of that is supported-runtime notebook evidence; the repository is not release-grade until a Colab/Kaggle execution for the exact candidate commit is recorded there.
